"""
중앙 규칙/사전 모듈
- 실행 로직과 분리된 '데이터(사전/리스트/파라미터)'만 모아둔다.
- 이후 parse_utils.py, units_dict.py 등이 여기의 값을 참조하도록 교체 예정.
"""

# -----------------------------
# 텍스트 치환/교정 규칙 (영양성분 명칭 오인식 등)
# -----------------------------
COMMON_MISREAD = {
    # 한글 오인식 교정
    "갈숨": "칼슘",
    "나트룹": "나트륨",
    "나트룸": "나트륨",
    "콜레스레돌": "콜레스테롤",
    "식이섭유": "식이섬유",
    "업산": "엽산",
    # 필요시 자유롭게 추가
}

# -----------------------------
# 단위 표기 원형 후보(대소문자/기호 포함)
# OCR이 토해낼 수 있는 변형을 넓게 수용
# -----------------------------
UNIT_RAW = [
    # 질량/에너지
    "g", "mg", "µg", "μg", "ug", "mcg", "㎍",
    "kcal", "cal",

    # 비타민/레티놀 계열
    "iu", "IU",
    "re", "RE",
    "ugre", "ugRE",
]

# -----------------------------
# 퍼지 매칭 등 파서 기본 파라미터
# -----------------------------
FUZZY_NAME_CUTOFF = 0.72  # difflib.get_close_matches cutoff 기본값

# -----------------------------
# EasyOCR 토큰 필터링 파라미터
# -----------------------------
EASYOCR_CONFIDENCE_MIN = 0.30  # detail=1 결과에서 사용할 최소 신뢰도
EASYOCR_LOOKAHEAD_TOKENS = 7   # 성분명 이후 근접 탐색 토큰 개수
